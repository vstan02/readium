{
  "name": "readium",
  "description": "An online book sharing social networking service",
  "author": "Stan Vlad <vstan02@protonmail.com>",
  "license": "GPLV3",
  "scripts": {
    "install": "npm i --prefix core",
    "lint": "npm run lint --prefix core",
    "test": "npm run test --prefix core",
    "dev": "npm run dev --prefix core",
    "build": "npm run build --prefix core"
  },
  "dependencies": {
    "express-validator": "^6.12.0"
  },
  "devDependencies": {
    "@types/express-validator": "^3.0.0"
  }
}
